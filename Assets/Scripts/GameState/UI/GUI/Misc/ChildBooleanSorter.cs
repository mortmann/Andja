using UnityEngine;

namespace Andja {

    public class ChildBooleanSorter : MonoBehaviour {

        // Use this for initialization
        private void Start() {
        }

        // Update is called once per frame
        private void Update() {
        }
    }
}