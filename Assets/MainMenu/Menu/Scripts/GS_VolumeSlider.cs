using UnityEngine;
using UnityEngine.UI;
using UnityEngine.Audio;

public class GS_VolumeSlider : GS_SliderBase {

    // Empty for the moment. We need everything in SliderBase, but not sure
    // what else we need for volume sliders.

}
